name: asmr_poc
description: "ASMR Creator Platform - Multitrack + Background PoC"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_soloud: ^3.0.0
  audio_service: ^0.18.15
  audio_session: ^0.1.21
  rxdart: ^0.27.7

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
